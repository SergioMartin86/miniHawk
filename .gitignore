**/.vs/**
**/.vscode/
/build/**
**/ipch/**
Backup/
UpgradeLog.htm

*.ilk
*.il
*.tlog
*.obj
*.o
*.gcda
*.filters
*.opendb


# mupen64plus-win32-deps is a submodule, so ignores should be in that repo
/libmupen64plus/mupen64plus-win32-deps/SDL-1.2.14/docs
/libmupen64plus/mupen64plus-win32-deps/SDL-1.2.14/docs.html

*.opensdf
*.user
*.suo
*.sdf
*.ipch
*.pdb
.hg
.hgignore
.hgtags

/psx/octoshock/bizhawk/Win32
/psx/octoshock/bizhawk/x64
/psx/octoshock/bizhawk/*.opendb

**/StyleCop.Cache

.idea

/packages

launchSettings.json
minibox-diag.log
mono_crash.*.json

# scratch left by the engine test binaries when run from a checkout root
# (test_project and friends write their fixtures into the working directory)
/*.chimeraProject
/*.chimeraGreenZone
/*.cue
/*.iso
/*.hdd
/disc*.bin

# game data for local testing - never shipped, never committed
/roms/

# The cores are separate repositories now (docs/core-manager.md): Chimera ships
# none, builds none, and downloads them through File > Core Manager. They live
# outside this tree - ~/chimera-cores/<name> on the machine this was written on
# - and this only keeps the old path free for anyone who still clones one here.
/extern/cores/
